<%= form_for([@budget_domain, @budget_account]) do |f| %>
  <% if @budget_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@budget_account.errors.count, "error") %> prohibited this budget_account from being saved:</h2>

      <ul>
      <% @budget_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :currency_code %><br>
    <%= f.select :currency_code, BudgetAccount::CURRENCY_CODES %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
