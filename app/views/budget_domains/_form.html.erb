<%= form_for(@budget_domain) do |f| %>
  <% if @budget_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@budget_domain.errors.count, "error") %> prohibited this budget_domain from being saved:</h2>

      <ul>
      <% @budget_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
