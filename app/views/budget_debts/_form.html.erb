<%= form_for([@budget_domain, @budget_debt]) do |f| %>
  <% if @budget_debt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@budget_debt.errors.count, "error") %> prohibited this budget_debt from being saved:</h2>

      <ul>
      <% @budget_debt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :budget_account_id %><br>
    <%= f.select :budget_account_id, @budget_domain.accounts.map { |x| [x.name, x.id] } %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
